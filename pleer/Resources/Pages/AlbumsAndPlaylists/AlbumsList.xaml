<Page x:Class="pleer.Resources.Pages.AlbumsAndPlaylists.AlbumsList"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:pleer.Resources.Pages.AlbumsAndPlaylists"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d" >

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.8*"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        
        <ScrollViewer Style="{StaticResource DarkScrollViewer}">
            <StackPanel Name="MediaLibraryAlbumsList"/>
        </ScrollViewer>

        <StackPanel Name="NoAnyAlbumsPanel"
                    HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="Кажется у вас еще нет выпусков"
                       Style="{StaticResource SmallMainInfoPanel}"/>
            <TextBlock MouseLeftButtonUp="CreateNewAlbum_Click"
                       Name="CreateNewAlbum" Margin="0 10"
                       Text="Нажмите чтобы добавить"
                       HorizontalAlignment="Center" 
                       Style="{StaticResource FunctionalSmallInfoPanel}">
            </TextBlock>
        </StackPanel>

        <Frame Grid.Column="1" Name="AlbumContent"
               NavigationUIVisibility="Hidden"/>
    </Grid>
</Page>
