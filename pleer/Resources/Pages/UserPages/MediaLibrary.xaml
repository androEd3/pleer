<Page x:Class="pleer.Resources.Pages.UserPages.MediaLibrary"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:pleer.Resources.Pages.UserPages"
          xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
          mc:Ignorable="d">

    <Border Margin="5 0" Padding="5"
            Style="{StaticResource NonFunctionalField22}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="35"/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <TextBlock Text="Моя медиаетка" Margin="5 0"
                       HorizontalAlignment="Left" VerticalAlignment="Center" 
                       Style="{StaticResource SmallInfoPanel}"/>
            <Button Grid.Column="1" Margin="5 0"
                    HorizontalAlignment="Right" VerticalAlignment="Center"
                    Click="CreatePlaylistButton_Click"
                    Style="{StaticResource BaseFunctionButtonWithBorder}">
                <materialDesign:PackIcon Kind="Plus" Width="30" Height="30"/>
            </Button>

            <Frame Grid.ColumnSpan="2" Grid.Row="1"
                   Margin="0 5 0 0"
                   Name="MediaLibraryAlbumsList"
                   NavigationUIVisibility="Hidden"/>
        </Grid>
    </Border>
</Page>
